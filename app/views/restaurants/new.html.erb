<%= simple_form_for(@restaurant) do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :category, as: :radio_buttons, collection: ['chinese', 'italian', 'japanese', 'french', 'belgian'] %>
    <%= f.submit %>
<% end %>
